% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_alpha_.R
\name{pd_alpha_intensity}
\alias{pd_alpha_intensity}
\title{Calculate mean alpha intensity of drawn pixels in a pain drawing PNG}
\usage{
pd_alpha_intensity(png, min_alpha = 0)
}
\arguments{
\item{png}{A numeric array of dimensions \verb{[height, width, 4]} representing
an RGBA image. The fourth slice (\code{png[,, 4]}) must contain alpha values
in the range [0, 1]. Typically the \code{.png} list-column produced by
\code{\link[=pd_add_png]{pd_add_png()}}.}

\item{min_alpha}{A single numeric value in [0, 1\) used as a lower
(exclusive) threshold for filtering pixels. Only pixels with alpha
strictly greater than \code{min_alpha} are included in the mean. Defaults to
\code{0}, i.e. all drawn pixels. Increase (e.g. \code{0.05}) to exclude very faint
noise pixels.}
}
\value{
A single numeric value in (0, 1] representing the mean alpha of
drawn pixels, or \code{NA_real_} if no pixels exceed \code{min_alpha}.
}
\description{
Computes the mean alpha value across all pixels with alpha greater than
\code{min_alpha}. This captures how intensely a patient drew -- i.e. how dark
or opaque the strokes are -- independently of how much area was covered.
Use in combination with \code{\link[=pd_alpha_area]{pd_alpha_area()}} to distinguish between a small
intense drawing and a large light drawing.
}
\examples{
pd <- pd_json2pd("data-raw/two_geoms.json") |> pd_add_png()

# Mean intensity across all drawn pixels
pd_alpha_intensity(pd$.png[[1]])

# Exclude very faint pixels (alpha <= 0.05)
pd_alpha_intensity(pd$.png[[1]], min_alpha = 0.05)

# Use inside mutate() with map_dbl()
pd |> dplyr::mutate(intensity = purrr::map_dbl(.png, pd_alpha_intensity))

}
\seealso{
\code{\link[=pd_alpha_area]{pd_alpha_area()}} for the complementary area-based metric.
}
