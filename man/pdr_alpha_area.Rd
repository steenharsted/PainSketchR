% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdr_alpha_.R
\name{pdr_alpha_area}
\alias{pdr_alpha_area}
\title{Calculate the proportion of canvas covered in a pain drawing PNG}
\usage{
pdr_alpha_area(rgbas, alpha_range = c(0, 1))
}
\arguments{
\item{rgbas}{A list of numeric arrays, each of dimensions
\verb{[height, width, 4]} representing an RGBA image. The fourth slice
(\code{rgba[,, 4]}) must contain alpha values in the range [0, 1].
Typically the \code{rgba} list-column produced by \code{\link[=pdr_add_rgba]{pdr_add_rgba()}}.}

\item{alpha_range}{A numeric vector of length 2 specifying the lower and
upper bounds of the alpha range (lower is exclusive, upper is inclusive).
Defaults to \code{c(0, 1)}, i.e. all drawn pixels regardless of intensity.
Use e.g. \code{c(0.5, 1)} to restrict to heavily drawn pixels only.}
}
\value{
A numeric vector of length \code{length(rgbas)}. Each element is the
proportion of canvas pixels with alpha in
\verb{(alpha_range[1], alpha_range[2]]} -- i.e. strictly greater than the
lower bound and less than or equal to the upper bound.
}
\description{
Computes the proportion of total pixels whose alpha value falls strictly
within \code{alpha_range}. This captures how much of the canvas was drawn on,
independently of stroke intensity. Use in combination with
\code{\link[=pdr_alpha_intensity]{pdr_alpha_intensity()}} to distinguish between a small intense drawing and
a large light drawing.
}
\examples{
pd <- pdr_import_json("data-raw/two_geoms.json") |>
  dplyr::mutate(rgba = pdr_add_rgba(pick(everything())))

# Proportion of all drawn pixels (any alpha > 0)
pdr_alpha_area(pd$rgba)

# Proportion of heavily drawn pixels only (alpha > 0.5)
pdr_alpha_area(pd$rgba, alpha_range = c(0.5, 1))

# Use inside mutate()
pd |> dplyr::mutate(
  area         = pdr_alpha_area(rgba),
  area_over_50 = pdr_alpha_area(rgba, alpha_range = c(0.5, 1))
)
}
\seealso{
\code{\link[=pdr_alpha_intensity]{pdr_alpha_intensity()}} for the complementary intensity-based metric.
}
