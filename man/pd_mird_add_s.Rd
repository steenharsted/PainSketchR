% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_mird_add_s.R
\name{pd_mird_add_s}
\alias{pd_mird_add_s}
\title{Generate an \code{s} column of stroke information (just \code{i} indeces) from the \code{p} column.}
\usage{
pd_mird_add_s(p)
}
\arguments{
\item{p}{A list column where each element is a data frame with an \code{i} column}
}
\value{
A list column where each element is a data frame with the unique \code{i} values from the input
}
\description{
Generate an \code{s} column of stroke information (just \code{i} indeces) from the \code{p} column.
}
\examples{
pd_df <- tibble::tibble(id=c("A", "B", "C"), mird_string = c(NA, "(,293,201,300,202,303,202,305,202,308,202,),(,198,104,206,103,203,100,209,108,202,109,)", "(,335,215,336,215,),(,331,211,331,212,331,213,331,214,),(,341,263,),(,341,272,),(,341,287,)"))
pd_df <- pd_df |> dplyr::mutate(p = pd_mird2pd(mird_string, flip_y=TRUE))
pd_df <- pd_df |> dplyr::mutate(s = pd_mird_add_s(p))
}
