% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_poly_manage_overlaps.R
\name{pd_poly_manage_overlaps}
\alias{pd_poly_manage_overlaps}
\title{Manage overlapping markings in pain drawings}
\usage{
pd_poly_manage_overlaps(p, method = "merge")
}
\arguments{
\item{method}{A string -- eigher "merge", "split", "adoptA" or "adoptB" -- currently only "merge" is implemented}

\item{pd}{A valid pain drawing data structure -- see \link{pd_check_data} for details.}
}
\value{
A valid pain drawing data structure
}
\description{
This function will manage overlapping markings/strokes in pain drawings by the chosen method. The method \if{html}{\out{<chr>}} can be 'merge', 'split', 'adoptA' or 'adoptB'.
}
\details{
The \code{method} parameter determines the way overlaps are managed:
\itemize{
\item "merge" will create a union of pairwise overlapping markings -- note i) this will result in the first marking being extended as the union of the two and the second marking will be deleted, ii) any enclosed 'holes' generated by the union will be ignored and incorporated in the resulting marking.
\item "split" will subtract the overlapping area from both markings and create a new marking representing the overlap -- not currently implemented!
\item "adoptA" will retain the first of the markings unchanged, and subtract the overlap from the second marking -- not currently implemented!
\item "adoptB" will retain the second of the markings unchanged, and subtract the overlap from the first marking -- not currently implemented!
}
}
\examples{
pd_demo_data[1:5,] |> mutate(p = pd_poly_cleanup(p, delta=20)) |> mutate(p = pd_poly_manage_overlaps(p))

}
