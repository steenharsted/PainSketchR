% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdr_add_rgba.R
\name{pdr_add_rgba_single}
\alias{pdr_add_rgba_single}
\title{Convert a single-row pain drawing to an RGBA raster array}
\usage{
pdr_add_rgba_single(
  .data,
  method = "memory",
  clean_up = TRUE,
  dpi = 96,
  col_id = "id",
  col_s = "s",
  col_p = "p",
  col_w = "w",
  col_h = "h"
)
}
\arguments{
\item{.data}{A single-row pain drawing tibble as produced by
\code{\link[=pdr_import_json]{pdr_import_json()}}. Must contain columns \code{id}, \code{s}, \code{p}, \code{w}, and \code{h}
(or the names supplied via \verb{col_*} arguments).
Passing more than one row is an error.}

\item{method}{Character. Controls the rasterization back-end. Either:
\itemize{
\item \code{"memory"} (default) — renders in-memory via \code{\link[ragg:agg_capture]{ragg::agg_capture()}},
no temp file is written to disk.
\item \code{"file"} — saves to a temporary PNG via \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} and reads
it back with \code{\link[png:readPNG]{png::readPNG()}}. Use this if the \code{"memory"} path produces
unexpected rendering artefacts.
}}

\item{clean_up}{Logical. If \code{TRUE} (default), the temporary PNG file is
deleted on exit. Only relevant when \code{method = "file"}. Set to \code{FALSE} to
retain the file for debugging.}

\item{dpi}{Resolution used during rasterization. Defaults to \code{96}, matching
the CSS pixel density of the web canvas where drawings are collected —
this ensures a 1:1 pixel correspondence between the original drawing and
the output. Increase for print-quality output (e.g. \code{300}), noting this
does not affect the canvas dimensions, only output pixel density.}

\item{col_id, col_s, col_p, col_w, col_h}{Column name strings for the drawing
ID, strokes list-column, points list-column, canvas width, and canvas
height respectively. Defaults match the output of \code{\link[=pdr_import_json]{pdr_import_json()}}:
\code{"id"}, \code{"s"}, \code{"p"}, \code{"w"}, \code{"h"}.}
}
\value{
A numeric array of dimensions \verb{height × width × 4} (RGBA channels,
values in [0, 1]). Transparent pixels are set to black
(\code{RGB = 0}) rather than transparent white.
}
\description{
Renders the strokes and spray points from a single-row pain drawing tibble
into an RGBA numeric array. This is the low-level workhorse called by
\code{\link[=pdr_add_rgba]{pdr_add_rgba()}}.
}
\examples{
\dontrun{
pd <- pdr_import_json("data-raw/two_geoms.json")
raster <- pdr_add_rgba_single(pd[1, ])
grid::grid.newpage()
grid::grid.raster(raster)
}

}
\seealso{
\code{\link[=pdr_add_rgba]{pdr_add_rgba()}} for the user-facing vectorised wrapper.
}
