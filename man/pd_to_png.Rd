% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_to_png.R
\name{pd_to_png}
\alias{pd_to_png}
\title{Render pain drawings to PNG raster arrays}
\usage{
pd_to_png(p = p, s = s, w = w, h = h, id = id, clean_up = TRUE, dpi = 96)
}
\arguments{
\item{p, s, w, h, id}{Tidy-selection expressions identifying the columns that
hold the point data, stroke data, canvas width, canvas height, and drawing
identifier respectively. Defaults match the column names produced by
\code{\link[=pd_json2pd]{pd_json2pd()}}.}

\item{clean_up}{Logical. If \code{TRUE} (default), temporary \code{.png} files are
deleted after each raster is read into memory. Passed through to
\code{\link[=pd_to_png_single]{pd_to_png_single()}}.}

\item{dpi}{Resolution passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} for each drawing.
Defaults to \code{96}, matching the CSS pixel density of the web canvas where
drawings are collected. All rows are rendered at the same \code{dpi}.}
}
\value{
A list of numeric arrays, one per row, each of dimensions
\verb{height × width × 4} (RGBA channels, values in [0, 1]).
}
\description{
Renders each row of a pain drawing tibble into a PNG raster array, returning
a list suitable for use as a \code{mutate()} column. Column arguments default to
the names produced by \code{\link[=pd_json2pd]{pd_json2pd()}}, so calling \code{pd_to_png()} bare inside
\code{mutate()} works without any extra arguments.
}
\examples{
\dontrun{
pd <- pd_json2pd(c("data-raw/two_geoms.json", "data-raw/four_geoms.json"))

# Bare call — column names match pd_json2pd() defaults
pd <- pd |> dplyr::mutate(.png = pd_to_png())

# Display the first drawing
grid::grid.newpage()
grid::grid.raster(pd$.png[[1]])
}

}
\seealso{
\code{\link[=pd_to_png_single]{pd_to_png_single()}} for the single-row primitive,
\code{\link[=pd_json2pd]{pd_json2pd()}} for reading pain drawing JSON files.
}
