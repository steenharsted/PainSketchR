% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_import_json.R
\name{pd_import_json}
\alias{pd_import_json}
\title{Import RedCap Pain Sketcher JSON data}
\usage{
pd_import_json(files)
}
\arguments{
\item{files}{Character vector (or list coercible to character) of file paths.}
}
\value{
A tibble representing a valid pain drawing data structure, or an empty
\code{data.frame()} if parsing fails.
}
\description{
Reads JSON files generated by the RedCap \emph{Pain Sketcher} module and converts
them into a pain drawing data structure (see \code{\link[=pdr_check_data]{pdr_check_data()}}).
}
\details{
Each input file is expected to contain a single pain drawing. Multiple files
can be provided and will be combined row-wise.

\strong{Expected JSON structure}

The input JSON must conform to the following structure:
\itemize{
\item A named list containing metadata for a single pain drawing, including:
\itemize{
\item \code{id} (character): unique drawing identifier
\item \code{s}: stroke data
}
\item \code{s} must be a data frame where each row represents a stroke and includes:
\itemize{
\item \code{i}: integer stroke identifier (unique within drawing)
\item \code{p}: list-column of coordinate matrices
}
\item Each element of \code{p} must be an integer matrix with two columns:
\itemize{
\item column 1: x-coordinates
\item column 2: y-coordinates
}
}

\strong{Transformation details}
\itemize{
\item Coordinates are converted into the internal \code{p} format (\code{i}, \code{x}, \code{y})
\item The y-axis is flipped using the drawing height (\code{h}) so that the origin
matches the internal coordinate system
\item Stroke metadata (\code{s}) and coordinates (\code{p}) are stored as list-columns
}

\strong{Failure handling}

If any of the following conditions occur, an empty \code{data.frame()} is returned:
\itemize{
\item \code{files} is not a character vector
\item a file does not exist
\item file contents are not valid JSON
}
}
\examples{
# Load a single file
#' pd_import_json(pd_demo_filepath_json_file_1)

# Load multiple files
pd_import_json(c(pd_demo_filepath_json_file_1,
             pd_demo_filepath_json_file_2))

# Plot imported data
pd_import_json(pd_demo_filepath_json_file_1) |>
  pd_recreate_drawing()
}
