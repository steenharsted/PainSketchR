% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_alpha_.R
\name{pd_alpha_area}
\alias{pd_alpha_area}
\title{Calculate the proportion of canvas covered in a pain drawing PNG}
\usage{
pd_alpha_area(png, alpha_range = c(0, 1))
}
\arguments{
\item{png}{A numeric array of dimensions \verb{[height, width, 4]} representing
an RGBA image. The fourth slice (\code{png[,, 4]}) must contain alpha values
in the range [0, 1]. Typically the \code{.png} list-column produced by
\code{\link[=pd_add_png]{pd_add_png()}}.}

\item{alpha_range}{A numeric vector of length 2 specifying the lower and
upper bounds of the alpha range (lower is exclusive, upper is inclusive). Defaults to \code{c(0, 1)},
i.e. all drawn pixels regardless of intensity. Use e.g. \code{c(0.5, 1)} to
restrict to heavily drawn pixels only.}
}
\value{
A single numeric value in [0, 1] representing the proportion of
canvas pixels with alpha in \verb{(alpha_range[1], alpha_range[2]]} --
i.e. strictly greater than the lower bound and less than or equal to
the upper bound.
}
\description{
Computes the proportion of total pixels whose alpha value falls strictly
within \code{alpha_range}. This captures how much of the canvas was drawn on,
independently of stroke intensity. Use in combination with
\code{\link[=pd_alpha_intensity]{pd_alpha_intensity()}} to distinguish between a small intense drawing and
a large light drawing.
}
\examples{
pd <- pd_json2pd("data-raw/two_geoms.json") |> pd_add_png()

# Proportion of all drawn pixels (any alpha > 0)
pd_alpha_area(pd$.png[[1]])

# Proportion of heavily drawn pixels only (alpha > 0.5)
pd_alpha_area(pd$.png[[1]], alpha_range = c(0.5, 1))

# Use inside mutate() with map_dbl()
pd |> dplyr::mutate(
  area         = purrr::map_dbl(.png, pd_alpha_area),
  area_over_50 = purrr::map_dbl(.png, \(x) pd_alpha_area(x, alpha_range = c(0.5, 1)))
)
}
\seealso{
\code{\link[=pd_alpha_intensity]{pd_alpha_intensity()}} for the complementary intensity-based metric.
}
