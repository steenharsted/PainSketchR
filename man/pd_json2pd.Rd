% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_json2pd.R
\name{pd_json2pd}
\alias{pd_json2pd}
\title{Load pain drawings from RedCap Pain Sketcher json data}
\usage{
pd_json2pd(files)
}
\arguments{
\item{files}{A list or vector of strings which represent file paths}
}
\value{
A pain drawing data structure -- see \link{pd_check_data} for more details.
}
\description{
This function reads data generated by the RedCap module 'Pain Sketcher' and converts it
into a valid pain drawing data structure -- see \link{pd_check_data} for more details about
\emph{valid pain drawing data structure}
}
\details{
The RedCap Pain Sketcher json data structure must adhere to these criteria:
\itemize{
\item It should be a list of named elements with meta data about the pain drawing. This must include 'id' \if{html}{\out{<chr>}} which is a unique pain drawing identifier and 's' which is a data frame, furthermore:
\item The element 's' must be a data frame with marking/stroke data, where each line represents a separate marking/stroke and this must include a column 'i' which is a unique identifier within that pain drawing and 'p' which is a list, furthermore:
\item The element 'p' must be list of \if{html}{\out{<int>}} matrixes of dimensions [1:n, 1:2] representing x and y corrdinates (col-wise)
}
}
\examples{
pd_json2pd(pd_demo_filepath_json_file_1) # Load data
pd_json2pd(pd_demo_filepath_json_file_1) |> pd_recreate_drawing() # Plot data
pd_json2pd(c(pd_demo_filepath_json_file_1, pd_demo_filepath_json_file_2))|> pd_recreate_drawing() # Plot data in two facets#' 

}
