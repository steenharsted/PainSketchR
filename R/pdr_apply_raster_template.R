pdr_apply_raster_template <- function(file, template_file, invert=FALSE) {
  # This function reads two png file and multiplies the raw
  # matrix RGBA values -- the idea is to allow for use of a
  # raster template to 'cut away' all pain drawing data in
  # the 'file' which is not inside (or outside) the template.

  
}